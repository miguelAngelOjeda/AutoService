<beans 
	xmlns="http://www.springframework.org/schema/beans" 
        xmlns:sec="http://www.springframework.org/schema/security"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
                    http://www.springframework.org/schema/beans/spring-beans.xsd
                    http://www.springframework.org/schema/security
                    http://www.springframework.org/schema/security/spring-security.xsd">

<sec:global-method-security secured-annotations="enabled" proxy-target-class="true"/>
    <http  auto-config="true"
      authentication-manager-ref="userITAuthenticationManager"
      xmlns="http://www.springframework.org/schema/security">
        
        <access-denied-handler error-page="/accesoDenegado"/>
        <intercept-url pattern="/accesoDenegado" access="IS_AUTHENTICATED_FULLY"/>
        <intercept-url pattern="/welcome" access="ROLE_USER, ROLE_ADMIN"/>
        <intercept-url pattern="/usuarios/**" access="ROLE_USER, ROLE_ADMIN"/>
        <intercept-url pattern="/usuarios**" access="ROLE_USER, ROLE_ADMIN"/>

        <form-login 
            login-page="/login"
            default-target-url="/welcome.jr"
            authentication-failure-url="/login?error=credenciales"
            username-parameter="formLogin:usuario"
            password-parameter="formLogin:password"/>
        <logout invalidate-session="true" logout-url="/logout" logout-success-url="/login"/>
        
    </http>

    <authentication-manager id="userITAuthenticationManager" xmlns="http://www.springframework.org/schema/security">
        <authentication-provider ref="customUserAuthenticationProvider"/>
    </authentication-manager>
    
    
</beans> 
